Frontend (Vite + React)

Install and run:

```bash
cd web
npm install
npm run dev
```

The frontend expects the backend at `/api/*` on the same host. For local development, run the server on port 4000 and use a proxy or start both with different terminals.
